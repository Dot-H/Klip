# Test environment configuration
# Used for local testing (port 5433 to avoid conflict with local postgres)
# CI uses port 5432 directly via workflow env vars

DATABASE_URL="postgresql://postgres:postgres@localhost:5433/klip_test"
NODE_ENV="test"
